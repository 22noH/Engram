# Engram Phase 0 Part 3 — 구현 계획 (KnowledgeCore 토대 완성)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** KnowledgeCore의 정합성·운영 토대를 닫는다 — 페이지별 락, 멀티유저 네임스페이스, unpublish 주경로, FTS optimize, 상주 위생(pino·lru-cache).

**Architecture:** 같은 페이지 쓰기를 `KeyedLock`으로 직렬화하고 git은 경로-스코프 add로 혼입을 막는다. 위키 페이지를 `wiki/pages/{userId}/`로 네임스페이싱하되 API는 `DEFAULT_USER` 기본값으로 단일사용자 호출을 그대로 유지한다. 임베딩은 lru-cache 데코레이터로 캐싱하고, 로깅은 pino로 디스크에 영속한다.

**Tech Stack:** NestJS 11 / TypeScript 5.7 / Node 22+ / LanceDB 0.30 / simple-git / chokidar 4 / jest 29. 신규: `pino`, `lru-cache`.

**Spec:** `docs/superpowers/specs/2026-06-24-phase0-part3-design.md`

## Global Constraints

- 셸 스크립트 0개 · OS cron 미사용 · 경로 하드코딩 금지(`path.join`).
- 모든 신규/수정 코드에 **한국어 주석**(전역 제약).
- 셸은 **PowerShell**(이 머신은 Bash 도구 깨짐). 테스트: `npx jest <경로>`.
- 신규 의존성은 **`pino`, `lru-cache`만**. 그 외 추가 금지.
- `DEFAULT_USER = 'default'` (PathResolver에서 export).
- userId는 모든 WikiEngine/RagStore 공개 메서드에 **후행 옵셔널**(`= DEFAULT_USER`). 기존 호출부 무수정.
- 기존 37 tests(+2 opt-in 임베더 skip) **회귀 0**, `npx tsc --noEmit`·`npm run build` 클린.
- 통합 임베더 테스트는 opt-in: `$env:ENGRAM_RAG_INTEGRATION=1` + `$env:NODE_OPTIONS='--experimental-vm-modules'`. 일반 테스트는 FakeEmbedder.
- 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

## 파일 구조

| 파일 | 책임 | 태스크 |
|---|---|---|
| `src/knowledge-core/keyed-lock.ts` (신설) | 키별 직렬화 락 | 1 |
| `src/pal/path-resolver.ts` (수정) | userId 네임스페이스·logs 경로 | 2 |
| `src/pal/logger.ts` (신설) | pino → 디스크 구조화 로깅 | 3 |
| `src/knowledge-core/rag/caching-embedder.ts` (신설) | lru-cache 임베딩 데코레이터 | 4 |
| `src/knowledge-core/wiki/wiki-git.ts` (수정) | 경로-스코프 커밋 | 5 |
| `src/knowledge-core/rag/rag.types.ts` (수정) | userId 필드/시그니처 | 6 |
| `src/knowledge-core/rag/rag-store.ts` (수정) | userId 색인·검색·optimize·마이그레이션 | 6 |
| `src/knowledge-core/wiki/wiki-engine.ts` (수정) | userId threading·unpublish·락 | 7,8 |
| `src/knowledge-core/rag/wiki-watcher.ts` (수정) | 멀티유저 경로 파싱·락·로거 | 9 |
| `src/knowledge-core/knowledge-core.module.ts` (수정) | provider wiring·onModuleInit | 8,9,10 |

---

### Task 1: KeyedLock

**Files:**
- Create: `src/knowledge-core/keyed-lock.ts`
- Test: `src/knowledge-core/keyed-lock.spec.ts`

**Interfaces:**
- Produces: `@Injectable class KeyedLock { run<T>(key: string, fn: () => Promise<T>): Promise<T> }` — 같은 key 직렬, 다른 key 독립, fn 성패 무관하게 체인 지속.

- [ ] **Step 1: 실패하는 테스트 작성** — `src/knowledge-core/keyed-lock.spec.ts`

```ts
import { KeyedLock } from './keyed-lock';

describe('KeyedLock', () => {
  it('같은 키는 직렬 실행된다(늦게 끝나는 작업이 먼저 완료)', async () => {
    const lock = new KeyedLock();
    const order: number[] = [];
    const first = lock.run('a', async () => {
      await new Promise((r) => setTimeout(r, 30));
      order.push(1);
    });
    const second = lock.run('a', async () => {
      order.push(2);
    });
    await Promise.all([first, second]);
    expect(order).toEqual([1, 2]);
  });

  it('다른 키는 병렬 진행한다', async () => {
    const lock = new KeyedLock();
    const order: string[] = [];
    const a = lock.run('a', async () => {
      await new Promise((r) => setTimeout(r, 30));
      order.push('a');
    });
    const b = lock.run('b', async () => {
      order.push('b');
    });
    await Promise.all([a, b]);
    expect(order).toEqual(['b', 'a']);
  });

  it('작업이 throw해도 같은 키의 다음 작업이 진행된다', async () => {
    const lock = new KeyedLock();
    await expect(
      lock.run('a', async () => {
        throw new Error('boom');
      }),
    ).rejects.toThrow('boom');
    await expect(lock.run('a', async () => 42)).resolves.toBe(42);
  });

  it('키의 마지막 작업 후 내부 맵을 정리한다(누수 방지)', async () => {
    const lock = new KeyedLock();
    await lock.run('a', async () => 1);
    await new Promise((r) => setTimeout(r, 0));
    expect((lock as unknown as { chains: Map<string, unknown> }).chains.size).toBe(0);
  });
});
```

- [ ] **Step 2: 실패 확인** — `npx jest src/knowledge-core/keyed-lock.spec.ts` → FAIL ("Cannot find module './keyed-lock'").

- [ ] **Step 3: 구현** — `src/knowledge-core/keyed-lock.ts`

```ts
import { Injectable } from '@nestjs/common';

// 키별 직렬화 락(설계 §10.3 "페이지 락"). 같은 key는 직전 작업이 끝난 뒤 실행되고,
// 다른 key는 서로 독립적으로 병렬 진행한다. 진짜 mutex가 아니라 Promise 체인이다.
@Injectable()
export class KeyedLock {
  private chains = new Map<string, Promise<unknown>>();

  // key에 대한 작업을 직렬 실행한다. fn의 성패와 무관하게 다음 작업이 이어진다.
  run<T>(key: string, fn: () => Promise<T>): Promise<T> {
    const prev = this.chains.get(key) ?? Promise.resolve();
    const result = prev.then(fn, fn);
    // 체인 꼬리(성패 흡수)를 기록하고, 이 key의 마지막 작업이면 맵에서 정리한다.
    const tail = result.then(
      () => undefined,
      () => undefined,
    );
    this.chains.set(key, tail);
    void tail.then(() => {
      if (this.chains.get(key) === tail) this.chains.delete(key);
    });
    return result;
  }
}
```

- [ ] **Step 4: 통과 확인** — `npx jest src/knowledge-core/keyed-lock.spec.ts` → PASS (4 tests).

- [ ] **Step 5: 커밋**

```
git add src/knowledge-core/keyed-lock.ts src/knowledge-core/keyed-lock.spec.ts
git commit -m "feat(core): KeyedLock — 키별 직렬화 락"
```

---

### Task 2: PathResolver — userId 네임스페이스 + logs 경로

**Files:**
- Modify: `src/pal/path-resolver.ts`
- Test: `src/pal/path-resolver.spec.ts`

**Interfaces:**
- Produces: `export const DEFAULT_USER = 'default'`; `getWikiPagesDir(userId?: string): string` → `wiki/pages/{userId}`; `getLogsDir(): string` → `runtime/logs`.

- [ ] **Step 1: 실패하는 테스트 추가** — `src/pal/path-resolver.spec.ts`에 추가(기존 import에 `DEFAULT_USER` 포함)

```ts
import { PathResolver, DEFAULT_USER } from './path-resolver';
import * as path from 'path';

// ... 기존 describe 안에 추가:
  it('getWikiPagesDir는 기본 사용자 네임스페이스를 포함한다', () => {
    const r = new PathResolver(path.join('C:', 'data'));
    expect(r.getWikiPagesDir()).toBe(
      path.join('C:', 'data', 'wiki', 'pages', DEFAULT_USER),
    );
  });

  it('getWikiPagesDir는 주어진 userId로 네임스페이스를 만든다', () => {
    const r = new PathResolver(path.join('C:', 'data'));
    expect(r.getWikiPagesDir('alice')).toBe(
      path.join('C:', 'data', 'wiki', 'pages', 'alice'),
    );
  });

  it('getLogsDir는 runtime/logs를 가리킨다', () => {
    const r = new PathResolver(path.join('C:', 'data'));
    expect(r.getLogsDir()).toBe(path.join('C:', 'data', 'logs'));
  });
```

> 기존에 `getWikiPagesDir()`가 `wiki/pages`를 기대하던 단언이 있으면 `wiki/pages/${DEFAULT_USER}`로 갱신한다.

- [ ] **Step 2: 실패 확인** — `npx jest src/pal/path-resolver.spec.ts` → FAIL (`DEFAULT_USER` 없음 / 경로 불일치).

- [ ] **Step 3: 구현** — `src/pal/path-resolver.ts`

상단 import 아래에 추가:
```ts
// 단일사용자 기본 네임스페이스(설계 §15 wiki/pages/{userId}). 멀티유저는 userId를 명시.
export const DEFAULT_USER = 'default';
```

`getWikiPagesDir` 교체 + `getLogsDir` 신설:
```ts
  // 위키 페이지(.md) 디렉토리. 멀티유저 네임스페이스 wiki/pages/{userId}/ (설계 §15).
  getWikiPagesDir(userId: string = DEFAULT_USER): string {
    return path.join(this.getWikiDir(), 'pages', userId);
  }

  // 구조화 로그(pino) 디렉토리.
  getLogsDir(): string {
    return path.join(this.dataDir, 'logs');
  }
```

- [ ] **Step 4: 통과 확인** — `npx jest src/pal/path-resolver.spec.ts` → PASS.

- [ ] **Step 5: 커밋**

```
git add src/pal/path-resolver.ts src/pal/path-resolver.spec.ts
git commit -m "feat(pal): PathResolver userId 네임스페이스 + getLogsDir"
```

---

### Task 3: PinoLogger (pino 구조화 로깅)

**Files:**
- Create: `src/pal/logger.ts`
- Test: `src/pal/logger.spec.ts`
- Modify: `package.json` (pino 의존성)

**Interfaces:**
- Consumes: `PathResolver.getLogsDir()`.
- Produces: `@Injectable class PinoLogger implements LoggerService` — `log/error/warn/debug/verbose`, `runtime/logs/engram.log`에 JSON 라인 영속.

- [ ] **Step 1: pino 설치** — `npm i pino` (package.json/package-lock.json 갱신).

- [ ] **Step 2: 실패하는 테스트 작성** — `src/pal/logger.spec.ts`

```ts
import { PinoLogger } from './logger';
import { PathResolver } from './path-resolver';
import * as fs from 'fs/promises';
import * as os from 'os';
import * as path from 'path';

describe('PinoLogger', () => {
  const tmps: string[] = [];
  afterAll(async () => {
    for (const t of tmps) await fs.rm(t, { recursive: true, force: true });
  });

  it('로그를 runtime/logs/engram.log에 JSON으로 기록한다', async () => {
    const tmp = await fs.mkdtemp(path.join(os.tmpdir(), 'engram-log-'));
    tmps.push(tmp);
    const logger = new PinoLogger(new PathResolver(tmp));
    logger.log('hello-engram', 'TestCtx');
    await new Promise((r) => setTimeout(r, 30)); // 디스크 flush 여유
    const content = await fs.readFile(
      path.join(tmp, 'logs', 'engram.log'),
      'utf8',
    );
    expect(content).toContain('hello-engram');
    expect(content).toContain('TestCtx');
  });

  it('error는 throw 없이 기록된다', async () => {
    const tmp = await fs.mkdtemp(path.join(os.tmpdir(), 'engram-log-'));
    tmps.push(tmp);
    const logger = new PinoLogger(new PathResolver(tmp));
    expect(() => logger.error('boom', 'stack', 'Ctx')).not.toThrow();
  });
});
```

- [ ] **Step 3: 실패 확인** — `npx jest src/pal/logger.spec.ts` → FAIL ("Cannot find module './logger'").

- [ ] **Step 4: 구현** — `src/pal/logger.ts`

```ts
import { Injectable, LoggerService } from '@nestjs/common';
import pino, { Logger } from 'pino';
import * as path from 'path';
import { PathResolver } from './path-resolver';

// pino 구조화 로깅을 Nest LoggerService로 노출(설계 §10.3 "구조화 로깅 디스크 영속").
// runtime/logs/engram.log에 JSON 라인으로 영속한다. nestjs-pino는 HTTP 지향이라 직접 래핑.
// ponytail: sync 목적지 — 개인 위키는 로그 volume이 낮아 동기 쓰기로 충분(flush 레이스 제거).
//           핫패스 로깅이 생기면 sync:false + flush로 전환.
@Injectable()
export class PinoLogger implements LoggerService {
  private readonly logger: Logger;

  constructor(paths: PathResolver) {
    const dest = path.join(paths.getLogsDir(), 'engram.log');
    this.logger = pino(
      { level: process.env.ENGRAM_LOG_LEVEL ?? 'info' },
      pino.destination({ dest, mkdir: true, sync: true }),
    );
  }

  log(message: unknown, context?: string): void {
    this.logger.info({ context }, String(message));
  }
  error(message: unknown, trace?: string, context?: string): void {
    this.logger.error({ context, trace }, String(message));
  }
  warn(message: unknown, context?: string): void {
    this.logger.warn({ context }, String(message));
  }
  debug(message: unknown, context?: string): void {
    this.logger.debug({ context }, String(message));
  }
  verbose(message: unknown, context?: string): void {
    this.logger.trace({ context }, String(message));
  }
}
```

- [ ] **Step 5: 통과 확인** — `npx jest src/pal/logger.spec.ts` → PASS.

- [ ] **Step 6: 커밋**

```
git add src/pal/logger.ts src/pal/logger.spec.ts package.json package-lock.json
git commit -m "feat(pal): PinoLogger — pino 디스크 구조화 로깅"
```

---

### Task 4: CachingEmbedder (lru-cache 임베딩 데코레이터)

**Files:**
- Create: `src/knowledge-core/rag/caching-embedder.ts`
- Test: `src/knowledge-core/rag/caching-embedder.spec.ts`
- Modify: `package.json` (lru-cache 의존성)

**Interfaces:**
- Consumes: `IEmbedder` (`embedder.port.ts`), 테스트에 `FakeEmbedder`.
- Produces: `class CachingEmbedder implements IEmbedder { constructor(inner: IEmbedder, max?: number) }` — `dimensions`는 inner 위임, `embed`는 미스만 위임.

- [ ] **Step 1: lru-cache 설치** — `npm i lru-cache`.

- [ ] **Step 2: 실패하는 테스트 작성** — `src/knowledge-core/rag/caching-embedder.spec.ts`

```ts
import { CachingEmbedder } from './caching-embedder';
import { FakeEmbedder } from './fake-embedder';

describe('CachingEmbedder', () => {
  it('dimensions를 내부 임베더에 위임한다', () => {
    expect(new CachingEmbedder(new FakeEmbedder()).dimensions).toBe(64);
  });

  it('같은 text는 캐시 적중 — 내부 임베더를 재호출하지 않는다', async () => {
    const inner = new FakeEmbedder();
    const spy = jest.spyOn(inner, 'embed');
    const c = new CachingEmbedder(inner);
    const [a] = await c.embed(['hello']);
    const [b] = await c.embed(['hello']);
    expect(a).toEqual(b);
    expect(spy).toHaveBeenCalledTimes(1);
  });

  it('미스만 위임하고 입력 순서를 보존한다', async () => {
    const inner = new FakeEmbedder();
    const spy = jest.spyOn(inner, 'embed');
    const c = new CachingEmbedder(inner);
    await c.embed(['a']);
    spy.mockClear();
    const out = await c.embed(['a', 'b', 'a', 'c']);
    expect(spy).toHaveBeenCalledTimes(1);
    expect(spy).toHaveBeenCalledWith(['b', 'c']);
    expect(out).toHaveLength(4);
    expect(out[0]).toEqual(out[2]);
  });

  it('max 초과 시 가장 오래된 키를 축출한다', async () => {
    const inner = new FakeEmbedder();
    const spy = jest.spyOn(inner, 'embed');
    const c = new CachingEmbedder(inner, 2);
    await c.embed(['a']);
    await c.embed(['b']);
    await c.embed(['c']); // a 축출
    spy.mockClear();
    await c.embed(['a']); // 재호출(미스)
    expect(spy).toHaveBeenCalledWith(['a']);
  });
});
```

- [ ] **Step 3: 실패 확인** — `npx jest src/knowledge-core/rag/caching-embedder.spec.ts` → FAIL.

- [ ] **Step 4: 구현** — `src/knowledge-core/rag/caching-embedder.ts`

```ts
import { LRUCache } from 'lru-cache';
import { IEmbedder } from './embedder.port';

// 임베딩 캐시 데코레이터(설계 §10.3 "모든 캐시는 lru-cache, 크기 제한"). text→vector를
// 바운드 LRU로 캐싱해 미변경 청크·반복 쿼리의 재임베딩을 피한다. 정확성은 캐시와 무관.
export class CachingEmbedder implements IEmbedder {
  private readonly cache: LRUCache<string, number[]>;

  // ponytail: max는 메모리 바운드 knob(초과 시 LRU 축출, 정확성 무관). 기본 4096 청크
  //           (1024d×4byte ≈ 4KB/개 → 약 16MB). 적중률 낮으면 키우고, 메모리 빡빡하면 줄임.
  constructor(
    private readonly inner: IEmbedder,
    max = 4096,
  ) {
    this.cache = new LRUCache<string, number[]>({ max });
  }

  get dimensions(): number {
    return this.inner.dimensions;
  }

  async embed(texts: string[]): Promise<number[][]> {
    // 캐시 미스만 모아 한 번에 위임(배치 유지) 후 원래 순서로 재조립한다.
    const result: (number[] | undefined)[] = texts.map((t) => this.cache.get(t));
    const missIdx: number[] = [];
    const missTexts: string[] = [];
    result.forEach((v, i) => {
      if (v === undefined) {
        missIdx.push(i);
        missTexts.push(texts[i]);
      }
    });
    if (missTexts.length > 0) {
      const embedded = await this.inner.embed(missTexts);
      embedded.forEach((vec, j) => {
        this.cache.set(texts[missIdx[j]], vec);
        result[missIdx[j]] = vec;
      });
    }
    return result as number[][];
  }
}
```

- [ ] **Step 5: 통과 확인** — `npx jest src/knowledge-core/rag/caching-embedder.spec.ts` → PASS.

- [ ] **Step 6: 커밋**

```
git add src/knowledge-core/rag/caching-embedder.ts src/knowledge-core/rag/caching-embedder.spec.ts package.json package-lock.json
git commit -m "feat(rag): CachingEmbedder — lru-cache 임베딩 데코레이터"
```

---

### Task 5: WikiGit — 경로-스코프 커밋

**Files:**
- Modify: `src/knowledge-core/wiki/wiki-git.ts`
- Test: `src/knowledge-core/wiki/wiki-git.spec.ts`

**Interfaces:**
- Produces: `commitAll(message: string, relPath?: string): Promise<void>` — `relPath` 주면 그 경로만 `add`, 없으면 `add('.')`(하위호환). 스테이징된 변경이 없으면 커밋 생략.

- [ ] **Step 1: 실패하는 테스트 추가** — `src/knowledge-core/wiki/wiki-git.spec.ts`

먼저 기존 `setup()`이 `paths`도 반환하도록 확장(기존 2개 테스트의 `{ engine, git }` 구조분해는 그대로 유효):
```ts
async function setup(): Promise<{ engine: WikiEngine; git: WikiGit; paths: PathResolver }> {
  const dir = await fs.mkdtemp(path.join(os.tmpdir(), 'engram-git-'));
  tmpDirs.push(dir);
  const paths = new PathResolver(dir);
  const git = new WikiGit(paths);
  await git.ensureRepo();
  const engine = new WikiEngine(paths, git);
  return { engine, git, paths };
}
```

파일 상단에 `import simpleGit from 'simple-git';` 추가. `describe` 안에 테스트 2개 추가:
```ts
  it('relPath를 주면 그 경로만 스테이징한다(동시 변경 혼입 방지)', async () => {
    const { git, paths } = await setup();
    const wikiDir = paths.getWikiDir();
    const pagesDir = path.join(wikiDir, 'pages', 'default');
    await fs.mkdir(pagesDir, { recursive: true });
    await fs.writeFile(path.join(pagesDir, 'x.md'), 'X');
    await fs.writeFile(path.join(pagesDir, 'y.md'), 'Y');
    await git.commitAll('add x only', path.join('pages', 'default', 'x.md'));
    const tracked = await simpleGit(wikiDir).raw(['ls-files']);
    expect(tracked).toContain('x.md'); // x만 커밋됨
    expect(tracked).not.toContain('y.md'); // y는 미커밋(워킹트리에 남음)
  });

  it('스테이징된 변경이 없으면 커밋하지 않는다', async () => {
    const { git, paths } = await setup();
    const wikiDir = paths.getWikiDir();
    const rel = path.join('pages', 'default', 'z.md');
    await fs.mkdir(path.join(wikiDir, 'pages', 'default'), { recursive: true });
    await fs.writeFile(path.join(wikiDir, rel), 'Z');
    await git.commitAll('add z', rel);
    const before = (await git.recentMessages(50)).length;
    await git.commitAll('noop z', rel); // 변경 없음 → 새 커밋 안 생김
    const after = (await git.recentMessages(50)).length;
    expect(after).toBe(before);
  });
```

- [ ] **Step 2: 실패 확인** — `npx jest src/knowledge-core/wiki/wiki-git.spec.ts` → 새 테스트 FAIL (relPath 인자 미지원).

- [ ] **Step 3: 구현** — `src/knowledge-core/wiki/wiki-git.ts`의 `commitAll` 교체

```ts
  // 위키 디렉토리의 변경을 커밋. relPath를 주면 그 경로만 스테이징(경로-스코프 — 동시 쓰기 혼입 방지,
  // 설계 §10.3/§11). relPath 미지정 시 전체(add('.')) — 하위호환. 스테이징된 변경이 없으면 커밋 생략.
  async commitAll(message: string, relPath?: string): Promise<void> {
    this.git.cwd(this.paths.getWikiDir());
    await this.git.add(relPath ?? '.');
    const status = await this.git.status();
    if (status.staged.length === 0) return; // 스테이징된 변경 없음 → 빈 커밋 방지
    await this.git.commit(message);
  }
```

`// TODO(Phase 0 Part 3): ...` 주석 블록은 제거한다(이 태스크가 그 TODO를 해소).

- [ ] **Step 4: 통과 확인** — `npx jest src/knowledge-core/wiki/wiki-git.spec.ts` → PASS (기존 + 신규 모두).

- [ ] **Step 5: 커밋**

```
git add src/knowledge-core/wiki/wiki-git.ts src/knowledge-core/wiki/wiki-git.spec.ts
git commit -m "feat(wiki): WikiGit 경로-스코프 커밋(혼입 방지)"
```

---

### Task 6: rag.types userId + RagStore 멀티유저·optimize

**Files:**
- Modify: `src/knowledge-core/rag/rag.types.ts`
- Modify: `src/knowledge-core/rag/rag-store.ts`
- Test: `src/knowledge-core/rag/rag-store.spec.ts`

**Interfaces:**
- Produces:
  - `IndexablePage { userId?: string; slug; title; category; sources; body }`
  - `SearchResult { userId?: string; slug; title; text; score }`
  - `PageIndexer.removePage(slug: string, userId?: string)`
  - `RagStore.search(query: string, limit?: number, userId?: string)` — userId 격리.
  - 색인: schema에 `userId` 컬럼, chunk id = `${userId}/${slug}#${i}`, 멱등 delete = `userId=? AND slug=?`, 쓰기마다 `optimize()`.
- Consumes: `DEFAULT_USER`(path-resolver).

- [ ] **Step 1: 실패하는 테스트 추가** — `src/knowledge-core/rag/rag-store.spec.ts` (기존 FakeEmbedder 하베스 사용)

```ts
  it('다른 userId의 같은 slug를 격리한다', async () => {
    await store.indexPage({
      userId: 'alice', slug: 'note', title: 'A', category: 'c', sources: [], body: 'apple pie recipe',
    });
    await store.indexPage({
      userId: 'bob', slug: 'note', title: 'B', category: 'c', sources: [], body: 'banana bread recipe',
    });
    const alice = await store.search('recipe', 5, 'alice');
    expect(alice.length).toBeGreaterThan(0);
    expect(alice.every((h) => h.text.includes('apple'))).toBe(true);
    expect(alice.some((h) => h.text.includes('banana'))).toBe(false);
  });

  it('removePage는 userId 범위로만 제거한다', async () => {
    await store.indexPage({
      userId: 'alice', slug: 'k', title: 'A', category: 'c', sources: [], body: 'keepme alpha',
    });
    await store.indexPage({
      userId: 'bob', slug: 'k', title: 'B', category: 'c', sources: [], body: 'keepme beta',
    });
    await store.removePage('k', 'alice');
    const bob = await store.search('keepme', 5, 'bob');
    expect(bob.some((h) => h.text.includes('beta'))).toBe(true);
    const alice = await store.search('keepme', 5, 'alice');
    expect(alice.some((h) => h.text.includes('alpha'))).toBe(false);
  });

  it('userId 미지정 색인·검색은 DEFAULT_USER로 동작한다(하위호환)', async () => {
    await store.indexPage({
      slug: 'legacy', title: 'L', category: 'c', sources: [], body: 'legacy default user content',
    });
    const hits = await store.search('legacy content'); // userId 생략
    expect(hits.length).toBeGreaterThan(0);
  });
```

> 기존 단일사용자 테스트(userId 없는 indexPage/search)는 그대로 통과해야 한다(하위호환 회귀 체크).

- [ ] **Step 2: 실패 확인** — `npx jest src/knowledge-core/rag/rag-store.spec.ts` → 신규 FAIL (userId 미지원·격리 안 됨).

- [ ] **Step 3: rag.types 수정** — `src/knowledge-core/rag/rag.types.ts`

```ts
export interface IndexablePage {
  userId?: string; // 멀티유저 네임스페이스. 미지정 시 RagStore가 DEFAULT_USER로 처리.
  slug: string;
  title: string;
  category: string;
  sources: string[];
  body: string;
}

export interface SearchResult {
  userId?: string;
  slug: string;
  title: string;
  text: string;
  score: number;
}

export interface PageIndexer {
  indexPage(page: IndexablePage): Promise<void>;
  removePage(slug: string, userId?: string): Promise<void>;
  reindexAll(pages: IndexablePage[]): Promise<void>;
}
```

- [ ] **Step 4: RagStore 구현** — `src/knowledge-core/rag/rag-store.ts`

import에 추가: `import { DEFAULT_USER } from '../../pal/path-resolver';`

`schema()`에 userId 필드 추가(맨 앞 권장):
```ts
      new Field('userId', new Utf8()),
```

`init()`의 기존 테이블 분기에 마이그레이션 추가:
```ts
    if (names.includes(TABLE)) {
      this.table = await this.db.openTable(TABLE);
      // 멀티유저 마이그레이션: userId 컬럼이 없는 구 스키마면 drop+recreate.
      // RAG는 wiki에서 파생·시작 시 reindex되므로 데이터 손실이 없다(disposable store).
      const fields = (await this.table.schema()).fields;
      if (!fields.some((f) => f.name === 'userId')) {
        await this.db.dropTable(TABLE);
        this.table = await this.db.createEmptyTable(TABLE, this.schema());
      }
    } else {
      this.table = await this.db.createEmptyTable(TABLE, this.schema());
    }
```

`indexPage` 교체:
```ts
  async indexPage(page: IndexablePage): Promise<void> {
    const userId = page.userId ?? DEFAULT_USER;
    return this.enqueue(async () => {
      // 멱등: 같은 (userId, slug)의 기존 청크 제거
      await this.table.delete(`userId = ${sql(userId)} AND slug = ${sql(page.slug)}`);
      const chunks = chunkBody(page.body);
      if (chunks.length === 0) return;
      const vectors = await this.embedder.embed(chunks);
      const now = new Date().toISOString();
      const rows = chunks.map((text, i) => ({
        id: `${userId}/${page.slug}#${i}`,
        userId,
        slug: page.slug,
        chunkIndex: i,
        title: page.title,
        category: page.category,
        text,
        vector: vectors[i],
        sources: JSON.stringify(page.sources),
        updated: now,
      }));
      await this.table.add(rows);
      await this.ensureFts();
      await this.table.optimize(); // 쓰기마다 FTS 인덱스 최신화(성능 정비, 정확성 무관)
    });
  }
```

`removePage` 교체:
```ts
  async removePage(slug: string, userId: string = DEFAULT_USER): Promise<void> {
    await this.enqueue(async () => {
      await this.table.delete(`userId = ${sql(userId)} AND slug = ${sql(slug)}`);
      // ponytail: 삭제 후에도 optimize로 tombstone 정비. startup reindexAll이 페이지마다
      //           optimize → 코퍼스 수백+ 시 배치/주기 인덱스로 승격(측정 후, YAGNI).
      await this.table.optimize();
    });
  }
```

`search` 시그니처·필터 교체:
```ts
  async search(query: string, limit = 5, userId: string = DEFAULT_USER): Promise<SearchResult[]> {
    const indices = await this.table.listIndices();
    if (!indices.some((idx) => idx.columns.includes('text'))) return [];
    const [qvec] = await this.embedder.embed([query]);
    const rows = (await this.table
      .query()
      .where(`userId = ${sql(userId)}`) // 사용자 격리(프리필터)
      .nearestTo(qvec)
      .fullTextSearch(query)
      .rerank(this.reranker)
      .limit(limit)
      .toArray()) as Array<Record<string, unknown>>;
    return rows.map((r) => ({
      userId: String(r.userId),
      slug: String(r.slug),
      title: String(r.title),
      text: String(r.text),
      score:
        r._relevance_score != null
          ? Number(r._relevance_score)
          : r._distance != null
            ? 1 / (1 + Number(r._distance))
            : 0,
    }));
  }
```

> **확인 항목(구현 중):** `.where()`가 하이브리드(벡터+FTS) 양쪽 leg에 프리필터로 적용되는지 LanceDB 0.30 동작을 실측한다(격리 테스트로 검증됨). 만약 FTS leg가 where를 무시하면, `.toArray()` 결과를 `r.userId === userId`로 **후필터**하고 limit를 적용하는 폴백으로 전환한다.

- [ ] **Step 5: 통과 확인** — `npx jest src/knowledge-core/rag/rag-store.spec.ts` → PASS (기존 + 신규). 격리 테스트 실패 시 위 폴백 적용 후 재확인.

- [ ] **Step 6: 커밋**

```
git add src/knowledge-core/rag/rag.types.ts src/knowledge-core/rag/rag-store.ts src/knowledge-core/rag/rag-store.spec.ts
git commit -m "feat(rag): RagStore 멀티유저 격리 + 쓰기마다 optimize"
```

---

### Task 7: WikiEngine — userId threading + unpublishPage

**Files:**
- Modify: `src/knowledge-core/wiki/wiki-engine.ts`
- Test: `src/knowledge-core/wiki/wiki-engine.spec.ts`

**Interfaces:**
- Consumes: `DEFAULT_USER`, `commitAll(message, relPath)`(Task 5), `PageIndexer.removePage(slug, userId)`(Task 6).
- Produces: `createPage(input, userId?)`, `getPage(slug, userId?)`, `updatePage(slug, patch, userId?)`, `listPages(filter?, userId?)`, `publishPage(slug, userId?)`, `unpublishPage(slug, userId?)`. 모두 `wiki/pages/{userId}/{slug}.md` + 경로-스코프 커밋. published만 색인.

- [ ] **Step 1: 실패하는 테스트 추가** — `src/knowledge-core/wiki/wiki-engine.spec.ts`

```ts
import { DEFAULT_USER } from '../../pal/path-resolver';
// ... describe 안에:

  it('다른 userId의 같은 slug를 파일로 격리한다', async () => {
    await engine.createPage({ slug: 'note', title: 'A', category: 'c', body: 'alice body' }, 'alice');
    await engine.createPage({ slug: 'note', title: 'B', category: 'c', body: 'bob body' }, 'bob');
    const a = await engine.getPage('note', 'alice');
    const b = await engine.getPage('note', 'bob');
    expect(a?.body).toBe('alice body');
    expect(b?.body).toBe('bob body');
  });

  it('userId 미지정은 DEFAULT_USER로 동작한다(하위호환)', async () => {
    await engine.createPage({ slug: 'legacy', title: 'L', category: 'c', body: 'x' });
    const viaDefault = await engine.getPage('legacy', DEFAULT_USER);
    expect(viaDefault?.slug).toBe('legacy');
  });

  it('unpublishPage는 published를 draft로 내리고 색인에서 제거한다', async () => {
    const removed: Array<[string, string | undefined]> = [];
    const indexer = {
      indexPage: jest.fn(async () => {}),
      removePage: jest.fn(async (slug: string, userId?: string) => {
        removed.push([slug, userId]);
      }),
      reindexAll: jest.fn(async () => {}),
    };
    const eng = makeEngine(indexer); // 기존 spec의 헬퍼: indexer 주입한 WikiEngine 생성
    await eng.createPage({ slug: 'p', title: 'P', category: 'c', body: 'b', status: 'published' });
    const result = await eng.unpublishPage('p');
    expect(result.frontmatter.status).toBe('draft');
    expect(indexer.removePage).toHaveBeenCalledWith('p', DEFAULT_USER);
  });

  it('unpublishPage는 이미 draft면 멱등 no-op으로 반환한다', async () => {
    const indexer = {
      indexPage: jest.fn(async () => {}),
      removePage: jest.fn(async () => {}),
      reindexAll: jest.fn(async () => {}),
    };
    const eng = makeEngine(indexer);
    await eng.createPage({ slug: 'd', title: 'D', category: 'c', body: 'b' }); // 기본 draft
    const result = await eng.unpublishPage('d');
    expect(result.frontmatter.status).toBe('draft');
    expect(indexer.removePage).not.toHaveBeenCalled();
  });
```

> 기존 spec에 indexer 주입 헬퍼가 없으면, 기존 테스트가 WikiEngine를 생성하는 방식을 그대로 따와 `makeEngine(indexer?)` 로컬 헬퍼를 만든다(PathResolver=temp, WikiGit, @Optional indexer).

- [ ] **Step 2: 실패 확인** — `npx jest src/knowledge-core/wiki/wiki-engine.spec.ts` → 신규 FAIL.

- [ ] **Step 3: 구현** — `src/knowledge-core/wiki/wiki-engine.ts`

import에 `DEFAULT_USER` 추가: `import { PathResolver, DEFAULT_USER } from '../../pal/path-resolver';`

`toIndexable`·`pagePath`에 userId 반영 + 경로-스코프용 상대경로 헬퍼:
```ts
  private toIndexable(page: WikiPage, userId: string): IndexablePage {
    return {
      userId,
      slug: page.slug,
      title: page.frontmatter.title,
      category: page.frontmatter.category,
      sources: page.frontmatter.sources,
      body: page.body,
    };
  }

  private pagePath(slug: string, userId: string): string {
    return path.join(this.paths.getWikiPagesDir(userId), `${slug}.md`);
  }

  // wiki 루트 기준 상대경로(경로-스코프 커밋용).
  private relPath(slug: string, userId: string): string {
    return path.relative(this.paths.getWikiDir(), this.pagePath(slug, userId));
  }
```

각 메서드에 userId 후행 옵셔널 + 경로-스코프 커밋 적용. 교체본:
```ts
  async createPage(input: CreatePageInput, userId: string = DEFAULT_USER): Promise<WikiPage> {
    const now = new Date().toISOString();
    const page: WikiPage = {
      slug: input.slug,
      frontmatter: {
        title: input.title,
        category: input.category,
        status: input.status ?? 'draft',
        sources: input.sources ?? [],
        created: now,
        updated: now,
      },
      body: input.body,
    };
    await fs.mkdir(this.paths.getWikiPagesDir(userId), { recursive: true });
    await fs.writeFile(this.pagePath(input.slug, userId), serializePage(page), { flag: 'wx' });
    await this.git.commitAll(`create ${userId}/${input.slug}`, this.relPath(input.slug, userId));
    if (page.frontmatter.status === 'published') {
      await this.indexer?.indexPage(this.toIndexable(page, userId));
    }
    return page;
  }

  async getPage(slug: string, userId: string = DEFAULT_USER): Promise<WikiPage | null> {
    try {
      const content = await fs.readFile(this.pagePath(slug, userId), 'utf8');
      return parsePage(slug, content);
    } catch (err) {
      if ((err as NodeJS.ErrnoException).code === 'ENOENT') return null;
      throw err;
    }
  }

  async updatePage(slug: string, patch: UpdatePageInput, userId: string = DEFAULT_USER): Promise<WikiPage> {
    const existing = await this.getPage(slug, userId);
    if (!existing) throw new Error(`Page not found: ${userId}/${slug}`);
    const updated: WikiPage = {
      slug,
      frontmatter: {
        ...existing.frontmatter,
        title: patch.title ?? existing.frontmatter.title,
        category: patch.category ?? existing.frontmatter.category,
        sources: patch.sources ?? existing.frontmatter.sources,
        updated: new Date().toISOString(),
      },
      body: patch.body ?? existing.body,
    };
    await fs.writeFile(this.pagePath(slug, userId), serializePage(updated));
    await this.git.commitAll(`update ${userId}/${slug}`, this.relPath(slug, userId));
    if (updated.frontmatter.status === 'published') {
      await this.indexer?.indexPage(this.toIndexable(updated, userId));
    }
    return updated;
  }

  async listPages(
    filter?: { status?: PageStatus },
    userId: string = DEFAULT_USER,
  ): Promise<WikiPage[]> {
    const dir = this.paths.getWikiPagesDir(userId);
    let files: string[];
    try {
      files = await fs.readdir(dir);
    } catch (err) {
      if ((err as NodeJS.ErrnoException).code === 'ENOENT') return [];
      throw err;
    }
    const pages: WikiPage[] = [];
    for (const f of files) {
      if (!f.endsWith('.md')) continue;
      const page = await this.getPage(f.slice(0, -3), userId);
      if (!page) continue;
      if (filter?.status && page.frontmatter.status !== filter.status) continue;
      pages.push(page);
    }
    return pages;
  }

  async publishPage(slug: string, userId: string = DEFAULT_USER): Promise<WikiPage> {
    const existing = await this.getPage(slug, userId);
    if (!existing) throw new Error(`Page not found: ${userId}/${slug}`);
    const published: WikiPage = {
      ...existing,
      frontmatter: { ...existing.frontmatter, status: 'published', updated: new Date().toISOString() },
    };
    await fs.writeFile(this.pagePath(slug, userId), serializePage(published));
    await this.git.commitAll(`publish ${userId}/${slug}`, this.relPath(slug, userId));
    await this.indexer?.indexPage(this.toIndexable(published, userId));
    return published;
  }

  // published → draft 강등(공개 취소). publishPage 대칭. 색인에서 제거한다(§5.2 stale 방지).
  async unpublishPage(slug: string, userId: string = DEFAULT_USER): Promise<WikiPage> {
    const existing = await this.getPage(slug, userId);
    if (!existing) throw new Error(`Page not found: ${userId}/${slug}`);
    if (existing.frontmatter.status === 'draft') return existing; // 멱등 no-op
    const draft: WikiPage = {
      ...existing,
      frontmatter: { ...existing.frontmatter, status: 'draft', updated: new Date().toISOString() },
    };
    await fs.writeFile(this.pagePath(slug, userId), serializePage(draft));
    await this.git.commitAll(`unpublish ${userId}/${slug}`, this.relPath(slug, userId));
    await this.indexer?.removePage(slug, userId);
    return draft;
  }
```

- [ ] **Step 4: 통과 확인** — `npx jest src/knowledge-core/wiki/wiki-engine.spec.ts` → PASS (기존 + 신규).

- [ ] **Step 5: 커밋**

```
git add src/knowledge-core/wiki/wiki-engine.ts src/knowledge-core/wiki/wiki-engine.spec.ts
git commit -m "feat(wiki): WikiEngine userId 네임스페이스 + unpublishPage"
```

---

### Task 8: WikiEngine 쓰기 락 적용 + 모듈 KeyedLock provider

**Files:**
- Modify: `src/knowledge-core/wiki/wiki-engine.ts`
- Modify: `src/knowledge-core/knowledge-core.module.ts`
- Test: `src/knowledge-core/wiki/wiki-engine.spec.ts`

**Interfaces:**
- Consumes: `KeyedLock`(Task 1).
- Produces: WikiEngine 쓰기 메서드(create/update/publish/unpublish)가 `lock.run(`${userId}/${slug}`, ...)` 경유. KeyedLock는 생성자 주입(`@Optional` 아님 — 모듈이 제공).

- [ ] **Step 1: 실패하는 테스트 추가** — `src/knowledge-core/wiki/wiki-engine.spec.ts`

```ts
import { KeyedLock } from '../keyed-lock';
// ...
  it('같은 (userId,slug) 동시 update는 직렬화되어 lost-update가 없다', async () => {
    await engine.createPage({ slug: 'race', title: 'R', category: 'c', body: 'start' });
    // 서로 다른 필드를 동시에 patch. 락이 read-modify-write를 직렬화하면 두 갱신이 모두 보존된다.
    // (락 없으면 둘 다 'start'/'R'를 읽어 마지막 writer가 다른 쪽 갱신을 덮어씀 = lost-update.)
    await Promise.all([
      engine.updatePage('race', { body: 'first' }),
      engine.updatePage('race', { title: 'Second' }),
    ]);
    const page = await engine.getPage('race');
    expect(page!.body).toBe('first'); // 두 갱신이 모두 살아남음(실행 순서 무관)
    expect(page!.frontmatter.title).toBe('Second');
  });
```

> `engine`은 KeyedLock를 주입해 생성해야 한다 — 기존 헬퍼/생성부에 `new KeyedLock()` 인자를 추가한다.

- [ ] **Step 2: 실패 확인** — `npx jest src/knowledge-core/wiki/wiki-engine.spec.ts` → FAIL (KeyedLock 생성자 인자 미존재 → 컴파일/런타임 에러).

- [ ] **Step 3: 구현 — WikiEngine 생성자 + 락 래핑**

생성자에 KeyedLock 주입:
```ts
import { KeyedLock } from '../keyed-lock';
// ...
  constructor(
    private readonly paths: PathResolver,
    private readonly git: WikiGit,
    private readonly lock: KeyedLock,
    @Optional() @Inject(PAGE_INDEXER) private readonly indexer?: PageIndexer,
  ) {}
```

쓰기 메서드 4개(create/update/publish/unpublish) 본문을 `lock.run`으로 감싼다. 예시(createPage):
```ts
  async createPage(input: CreatePageInput, userId: string = DEFAULT_USER): Promise<WikiPage> {
    return this.lock.run(`${userId}/${input.slug}`, async () => {
      // ... 기존 본문(파일 쓰기 → commitAll → 색인) 그대로 ...
      return page;
    });
  }
```
같은 방식으로 `updatePage`/`publishPage`/`unpublishPage` 본문을 `this.lock.run(`${userId}/${slug}`, async () => { ... })`로 감싼다. `getPage`/`listPages`(읽기)는 감싸지 않는다.

- [ ] **Step 4: 모듈에 KeyedLock provider 추가** — `src/knowledge-core/knowledge-core.module.ts`

```ts
import { KeyedLock } from './keyed-lock';
// providers 배열에 추가:
    KeyedLock,
```

- [ ] **Step 5: 통과 확인** — `npx jest src/knowledge-core/wiki/wiki-engine.spec.ts` 및 `npx jest src/knowledge-core/knowledge-core.module.spec.ts` → PASS. `npx tsc --noEmit` 클린.

- [ ] **Step 6: 커밋**

```
git add src/knowledge-core/wiki/wiki-engine.ts src/knowledge-core/wiki/wiki-engine.spec.ts src/knowledge-core/knowledge-core.module.ts
git commit -m "feat(wiki): 페이지별 락으로 쓰기 직렬화(KeyedLock)"
```

---

### Task 9: WikiWatcher — 멀티유저 경로 파싱 + 락 + 로거

**Files:**
- Modify: `src/knowledge-core/rag/wiki-watcher.ts`
- Modify: `src/knowledge-core/knowledge-core.module.ts` (PinoLogger provider)
- Test: `src/knowledge-core/rag/wiki-watcher.spec.ts`

**Interfaces:**
- Consumes: `KeyedLock`, `PinoLogger`, `DEFAULT_USER`.
- Produces: 워처가 `wiki/pages` 하위 모든 `{userId}/{slug}.md`를 감시하고, `handleChange(userId, slug, event)`로 (userId, slug)별 색인/제거를 같은 KeyedLock 키로 수행. `console.error` → 로거.

- [ ] **Step 1: 실패하는 테스트 추가** — `src/knowledge-core/rag/wiki-watcher.spec.ts`

```ts
import { DEFAULT_USER } from '../../pal/path-resolver';
// ...
  it('파일 경로에서 userId와 slug를 파싱한다', () => {
    // pathFor: paths.getWikiDir()/pages/alice/note.md
    const file = path.join(paths.getWikiDir(), 'pages', 'alice', 'note.md');
    expect((watcher as any).parseFile(file)).toEqual({ userId: 'alice', slug: 'note' });
  });

  it('handleChange(unlink)는 해당 userId 범위로 removePage를 호출한다', async () => {
    await watcher.handleChange('alice', 'gone', 'unlink');
    expect(rag.removePage).toHaveBeenCalledWith('gone', 'alice');
  });

  it('published 페이지 change는 userId와 함께 indexPage를 호출한다', async () => {
    await wiki.createPage({ slug: 'p', title: 'P', category: 'c', body: 'b', status: 'published' }, 'alice');
    await watcher.handleChange('alice', 'p', 'change');
    expect(rag.indexPage).toHaveBeenCalledWith(
      expect.objectContaining({ userId: 'alice', slug: 'p' }),
    );
  });
```

> 기존 spec의 `rag`(목)·`wiki`·`watcher` 구성 방식을 따른다. watcher 생성 시 `KeyedLock`·`PinoLogger`(또는 목)를 주입하도록 생성부를 갱신한다.

- [ ] **Step 2: 실패 확인** — `npx jest src/knowledge-core/rag/wiki-watcher.spec.ts` → FAIL.

- [ ] **Step 3: 구현** — `src/knowledge-core/rag/wiki-watcher.ts`

생성자·감시 루트·파싱·핸들러 교체:
```ts
import { Injectable, OnModuleDestroy } from '@nestjs/common';
import chokidar, { FSWatcher } from 'chokidar';
import * as fs from 'fs/promises';
import * as path from 'path';
import { PathResolver, DEFAULT_USER } from '../../pal/path-resolver';
import { RagStore } from './rag-store';
import { WikiEngine } from '../wiki/wiki-engine';
import { KeyedLock } from '../keyed-lock';
import { PinoLogger } from '../../pal/logger';

const DEBOUNCE_MS = 300;

@Injectable()
export class WikiWatcher implements OnModuleDestroy {
  private watcher?: FSWatcher;
  private timers = new Map<string, NodeJS.Timeout>();

  constructor(
    private readonly paths: PathResolver,
    private readonly rag: RagStore,
    private readonly wiki: WikiEngine,
    private readonly lock: KeyedLock,
    private readonly logger: PinoLogger,
  ) {}

  async start(): Promise<void> {
    // wiki/pages 전체(모든 userId 하위)를 재귀 감시하고 .md만 필터.
    const root = path.dirname(this.paths.getWikiPagesDir(DEFAULT_USER)); // = wiki/pages
    await fs.mkdir(root, { recursive: true });
    this.watcher = chokidar.watch(root, {
      ignoreInitial: true,
      ignored: (p, stats) => !!stats?.isFile() && !p.endsWith('.md'),
    });
    this.watcher
      .on('add', (f) => this.schedule(f, 'change'))
      .on('change', (f) => this.schedule(f, 'change'))
      .on('unlink', (f) => this.schedule(f, 'unlink'));
  }

  async stop(): Promise<void> {
    for (const t of this.timers.values()) clearTimeout(t);
    this.timers.clear();
    await this.watcher?.close();
  }

  async onModuleDestroy(): Promise<void> {
    await this.stop();
  }

  // wiki/pages 기준 상대경로에서 {userId}/{slug}를 파싱한다.
  private parseFile(file: string): { userId: string; slug: string } | null {
    const root = path.dirname(this.paths.getWikiPagesDir(DEFAULT_USER));
    const rel = path.relative(root, file);
    const parts = rel.split(path.sep);
    if (parts.length !== 2 || !parts[1].endsWith('.md')) return null; // {userId}/{slug}.md만
    return { userId: parts[0], slug: parts[1].slice(0, -3) };
  }

  private schedule(file: string, event: 'change' | 'unlink'): void {
    const parsed = this.parseFile(file);
    if (!parsed) return;
    const key = `${parsed.userId}/${parsed.slug}`;
    const prev = this.timers.get(key);
    if (prev) clearTimeout(prev);
    this.timers.set(
      key,
      setTimeout(() => {
        this.timers.delete(key);
        void this.handleChange(parsed.userId, parsed.slug, event).catch((err) =>
          this.logger.error(`재색인 실패 ${key}`, String(err), 'WikiWatcher'),
        );
      }, DEBOUNCE_MS),
    );
  }

  // 같은 페이지 키로 락을 잡아 WikiEngine 동기색인과 조율한다(멱등 — 중복 색인 무해).
  async handleChange(userId: string, slug: string, event: 'change' | 'unlink'): Promise<void> {
    await this.lock.run(`${userId}/${slug}`, async () => {
      if (event === 'unlink') {
        await this.rag.removePage(slug, userId);
        return;
      }
      const page = await this.wiki.getPage(slug, userId);
      if (page && page.frontmatter.status === 'published') {
        await this.rag.indexPage({
          userId,
          slug: page.slug,
          title: page.frontmatter.title,
          category: page.frontmatter.category,
          sources: page.frontmatter.sources,
          body: page.body,
        });
      } else {
        await this.rag.removePage(slug, userId); // draft로 내려갔거나 사라짐 → 색인 제거
      }
    });
  }
}
```

- [ ] **Step 4: 모듈에 PinoLogger provider 추가** — `src/knowledge-core/knowledge-core.module.ts`

```ts
import { PinoLogger } from '../pal/logger';
// providers 배열에 추가:
    PinoLogger,
```

- [ ] **Step 5: 통과 확인** — `npx jest src/knowledge-core/rag/wiki-watcher.spec.ts` → PASS. `npx tsc --noEmit` 클린.

- [ ] **Step 6: 커밋**

```
git add src/knowledge-core/rag/wiki-watcher.ts src/knowledge-core/rag/wiki-watcher.spec.ts src/knowledge-core/knowledge-core.module.ts
git commit -m "feat(rag): WikiWatcher 멀티유저 경로 파싱 + 락 + 로거"
```

---

### Task 10: 모듈 wiring(CachingEmbedder·onModuleInit) + 통합·회귀

**Files:**
- Modify: `src/knowledge-core/knowledge-core.module.ts`
- Test: `src/knowledge-core/knowledge-core.module.spec.ts`

**Interfaces:**
- Consumes: `CachingEmbedder`(Task 4), `TransformersEmbedder`, `DEFAULT_USER`.
- Produces: `EMBEDDER` = TransformersEmbedder를 감싼 CachingEmbedder. `onModuleInit` reindex가 published 페이지를 userId와 함께 전달하고, init 전체를 try/catch로 보호.

- [ ] **Step 1: 실패하는 테스트 추가** — `src/knowledge-core/knowledge-core.module.spec.ts`

```ts
import { CachingEmbedder } from './rag/caching-embedder';
import { EMBEDDER } from './rag/embedder.port';
// ...
  it('EMBEDDER는 CachingEmbedder로 래핑된다', async () => {
    const moduleRef = await Test.createTestingModule({ imports: [KnowledgeCoreModule] })
      // 기존 spec과 동일하게 임베더는 Fake로 override할 수도 있으나, wiring 검증이 목적이면
      // EMBEDDER 토큰 해소 결과 타입을 확인한다.
      .compile();
    const embedder = moduleRef.get(EMBEDDER);
    expect(embedder).toBeInstanceOf(CachingEmbedder);
    await moduleRef.close();
  });
```

> 기존 module.spec이 무거운 실모델을 피하려고 EMBEDDER를 FakeEmbedder로 override한다면, 이 테스트는 별도 모듈 구성으로 wiring만 확인하거나, useFactory가 CachingEmbedder를 반환하는지 단위로 검증한다(실모델 로딩 회피).

- [ ] **Step 2: 실패 확인** — `npx jest src/knowledge-core/knowledge-core.module.spec.ts` → FAIL.

- [ ] **Step 3: 구현 — 모듈 wiring** — `src/knowledge-core/knowledge-core.module.ts`

```ts
import { CachingEmbedder } from './rag/caching-embedder';
import { DEFAULT_USER } from '../pal/path-resolver';
// EMBEDDER provider 교체: TransformersEmbedder를 CachingEmbedder로 감싼다.
    TransformersEmbedder,
    {
      provide: EMBEDDER,
      useFactory: (base: TransformersEmbedder) => new CachingEmbedder(base),
      inject: [TransformersEmbedder],
    },
```

`onModuleInit` reindex에 userId 부여 + try/catch:
```ts
  async onModuleInit(): Promise<void> {
    try {
      await this.git.ensureRepo();
      await this.rag.init();
      // 시작 시 모든 사용자 디렉토리의 published 페이지 재색인.
      // (현재 단일사용자 = DEFAULT_USER. 멀티유저 디렉토리 스캔은 사용자 추가 시 확장.)
      // reindexAll은 watcher.start() 전에 실행되므로 동시 쓰기원이 없다(락 불필요).
      const pages = await this.wiki.listPages({ status: 'published' }, DEFAULT_USER);
      await this.rag.reindexAll(
        pages.map((p) => ({
          userId: DEFAULT_USER,
          slug: p.slug,
          title: p.frontmatter.title,
          category: p.frontmatter.category,
          sources: p.frontmatter.sources,
          body: p.body,
        })),
      );
      await this.watcher.start();
    } catch (err) {
      // 한 단계 실패가 프로세스를 죽이지 않게(설계 §10.3). 데이터는 runtime/에 보존.
      this.logger.error('KnowledgeCore 초기화 실패', String(err), 'KnowledgeCoreModule');
      throw err;
    }
  }
```

생성자에 `PinoLogger` 주입(클래스에서 사용하도록):
```ts
  constructor(
    private readonly git: WikiGit,
    private readonly wiki: WikiEngine,
    private readonly rag: RagStore,
    private readonly watcher: WikiWatcher,
    private readonly logger: PinoLogger,
  ) {}
```

> **참고:** EMBEDDER가 `useClass: TransformersEmbedder`였던 기존 라인을 위 useFactory로 교체한다. `PAGE_INDEXER: useExisting RagStore`·`WikiWatcher`·`WikiEngine` 등 나머지 provider는 유지.

- [ ] **Step 4: 통과 확인** — `npx jest src/knowledge-core/knowledge-core.module.spec.ts` → PASS.

- [ ] **Step 5: 전체 회귀 + 빌드**

```
npx jest
npx tsc --noEmit
npm run build
```
기대: 기존 37 + 신규 테스트 모두 PASS(임베더 통합 2건은 opt-in skip), tsc/build 클린.

- [ ] **Step 6: 커밋**

```
git add src/knowledge-core/knowledge-core.module.ts src/knowledge-core/knowledge-core.module.spec.ts
git commit -m "feat(core): CachingEmbedder wiring + onModuleInit userId/try-catch"
```

---

## Self-Review (작성자 체크 — 완료)

- **Spec 커버리지:** 멀티유저(2,6,7,9,10) · 페이지별 락(1,8,9) · git 경로-스코프(5) · unpublish(7) · FTS optimize(6) · pino(3,9,10) · lru-cache(4,10) · try/catch(9,10) — 전부 태스크 매핑됨. 연기 항목(수집·한국어 토크나이저)은 의도적 비범위.
- **Placeholder:** 모든 코드 스텝에 실코드 포함, TBD/TODO 없음(Task 5가 기존 TODO 주석 제거).
- **타입 일관성:** `removePage(slug, userId?)`·`search(query, limit, userId?)`·`indexPage({userId,...})`·`commitAll(message, relPath?)`·`getPage(slug, userId?)`가 정의 태스크(5,6)와 소비 태스크(7,8,9,10) 전반에서 일치. `KeyedLock.run(key, fn)` 일관.
- **확인 항목(구현 중):** ① LanceDB `.where()`의 하이브리드 프리필터 적용(Task 6, 폴백=후필터 명시) ② `table.optimize()` 비용(Task 6 주석의 천장) ③ `table.schema().fields[].name`·`status.staged` 필드명은 LanceDB 0.30/simple-git 실측으로 확정.
